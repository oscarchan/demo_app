<%= form_for(@my_survey) do |f| %>
  <% if @my_survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_survey.errors.count, "error") %> prohibited this my_survey from being saved:</h2>

      <ul>
      <% @my_survey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
